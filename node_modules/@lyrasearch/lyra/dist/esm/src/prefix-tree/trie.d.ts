import { Node } from "./node";
export declare type Nodes = Record<string, Node>;
export declare type FindParams = {
    term: string;
    exact?: boolean;
    tolerance?: number;
};
export declare type FindResult = Record<string, string[]>;
export declare function insert(nodes: Nodes, node: Node, word: string, docId: string): void;
export declare function contains(nodes: Nodes, node: Node, word: string): boolean;
export declare function find(nodes: Nodes, node: Node, { term, exact, tolerance }: FindParams): FindResult;
export declare function removeDocumentByWord(nodes: Nodes, node: Node, word: string, docID: string, exact?: boolean): boolean;
export declare function removeWord(nodes: Nodes, node: Node, word: string): boolean;
